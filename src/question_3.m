% Generate the signal

Fs = 1200;
N = 1;
M = 1;
t = 0:100;
f = 0:Fs/2;     % all frequencies.
                % TODO: There needs to be M of them

noise = 0;

function_sum = 0; % placeholder

x = function_sum + noise;